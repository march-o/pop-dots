# Silence all bell behaviour (audio + tab indicator + compositor attention).
# SSH broken pipe sends a bell; without these, COSMIC snaps focus back and
# plays a sound on every disconnect.
enable_audio_bell no
window_alert_on_bell no
bell_on_tab no
